export enum Approach {
    Brute_Force = 'Brute_Force',
    Recursive = 'Recursive',
    Better = 'Better',
    Optimal = 'Optimal',
    Naive  = 'Naive'
}

export enum Direction {
    Left,
    Right
}

export enum Case {
    _1,
    _2,
    _3,
}

export enum  Using {
    Map = 'Map',
    Hash = 'Hash',
    Loop = 'Loop'
}