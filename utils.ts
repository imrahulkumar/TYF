export enum Approach {
    Brute_Force = 'Brute_Force',
    Recursive = 'Recursive',
    Better = 'Better',
    Optimal = 'Optimal'
}

export enum Direction {
    Left,
    Right
}

export enum Case {
    _1,
    _2,
    _3,
}